<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="0dp"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.androidcamp.jobbies.MainActivity"
    tools:showIn="@layout/app_bar_main">


    <Button
        style="@android:style/Widget.Button"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:textSize="36dp"
        android:textColor="@color/colorAccent"
        android:textStyle="bold"
        android:textAllCaps="true"
        android:background="@color/find_job_btn_bg"
        android:text="@string/find_job"
        android:onClick="onFindClickHandler"
        android:id="@+id/find_button"/>

    <Button
        style="@android:style/Widget.Button"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:textSize="36dp"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textAllCaps="true"
        android:background="@color/offer_job_btn_bg"
        android:text="@string/offer_job"
        android:onClick="onOfferClickHandler"
        android:id="@+id/offer_button"
        android:layout_below="@+id/find_button"/>

</LinearLayout>
